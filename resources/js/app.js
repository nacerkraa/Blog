require('./bootstrap');

/**
 * Library: jQuery
 * Offical WEBSite: https://jquery.com/
 * the following line of code imports the jquery library in all global application of laravel,
 * it was installed with the npm install command (npm i jquery).
 */
import jquery from 'jquery';
window.$ = window.jQuery = jquery;